import { describe, it } from 'vitest';

describe('routeTransition', () => {
  it.todo('should render a transition container when a transition object is provided');
  it.todo('should not render a transition container when a transition object is not provided');
});
